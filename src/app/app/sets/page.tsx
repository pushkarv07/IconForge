"use client";

import JSZip from "jszip";
import Link from "next/link";
import { Copy, Download, Pencil, Plus, Trash2 } from "lucide-react";
import { useEffect, useState } from "react";
import { defaultStyleProfile, loadSets, makeSet, saveSets } from "@/lib/sets";
import type { IconSet } from "@/lib/types";

function fileName(name: string) { return name.toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/(^-|-$)/g, "") || "icon"; }
async function downloadZip(set: IconSet) { const zip = new JSZip(); set.icons.forEach((icon) => zip.file(`${fileName(icon.name)}.svg`, icon.svg)); const blob = await zip.generateAsync({ type: "blob" }); const url = URL.createObjectURL(blob); const link = document.createElement("a"); link.href = url; link.download = `${fileName(set.name)}.zip`; link.click(); URL.revokeObjectURL(url); }

export default function SetsPage() {
  const [sets, setSets] = useState<IconSet[]>([]);
  const [dialogOpen, setDialogOpen] = useState(false);
  const [name, setName] = useState("");
  const [description, setDescription] = useState("");
  useEffect(() => setSets(loadSets()), []);
  function persist(next: IconSet[]) { setSets(next); saveSets(next); }
  function createSet(event: React.FormEvent) { event.preventDefault(); if (!name.trim()) return; persist([...sets, makeSet(name.trim(), description.trim(), defaultStyleProfile)]); setName(""); setDescription(""); setDialogOpen(false); }
  function rename(set: IconSet) { const nextName = window.prompt("Rename icon set", set.name); if (nextName?.trim()) persist(sets.map((item) => item.id === set.id ? { ...item, name: nextName.trim(), updatedAt: new Date().toISOString() } : item)); }
  function duplicate(set: IconSet) { persist([...sets, { ...set, id: `${Date.now()}`, name: `${set.name} copy`, icons: [...set.icons], updatedAt: new Date().toISOString() }]); }
  function remove(set: IconSet) { if (window.confirm(`Delete ${set.name}?`)) persist(sets.filter((item) => item.id !== set.id)); }
  return <main className="app-shell"><header className="topbar"><Link href="/" className="brand"><span className="brand-mark">IF</span> IconForge</Link><div style={{ display: "flex", gap: 8 }}><Link href="/create" className="btn btn-quiet">Workspace</Link><button className="btn btn-primary" onClick={() => setDialogOpen(true)}><Plus size={15} /> New set</button></div></header><div className="page-content"><div className="page-header"><div><div className="eyebrow">Your library</div><h1>Icon sets</h1><p className="muted" style={{ margin: 0 }}>Keep related icons together and consistent.</p></div><button className="btn btn-primary" onClick={() => setDialogOpen(true)}><Plus size={15} /> New set</button></div>{sets.length ? <div className="set-grid">{sets.map((set) => <article className="set-card" key={set.id}><Link href={`/sets/${set.id}`} className="set-card-link"><div className="set-icons">{set.icons.slice(0, 5).map((icon) => <span key={icon.id} dangerouslySetInnerHTML={{ __html: icon.svg }} />)}{!set.icons.length && <span className="muted" style={{ fontSize: 12 }}>No icons yet</span>}</div><div className="set-name">{set.name}</div>{set.description && <div className="set-meta">{set.description}</div>}<div className="set-meta">{set.icons.length} icons · {set.profile.canvas} × {set.profile.canvas} · {set.profile.stroke} px · {set.profile.fillMode}</div><div className="set-meta">Updated {set.updatedAt ? new Date(set.updatedAt).toLocaleDateString() : "recently"}</div></Link><div className="set-card-actions"><Link href={`/sets/${set.id}`} className="btn btn-quiet">Open</Link><button className="icon-btn" onClick={() => rename(set)} aria-label={`Rename ${set.name}`}><Pencil size={14} /></button><button className="icon-btn" onClick={() => duplicate(set)} aria-label={`Duplicate ${set.name}`}><Copy size={14} /></button><button className="icon-btn" onClick={() => downloadZip(set)} aria-label={`Export ${set.name}`}><Download size={14} /></button><button className="icon-btn" onClick={() => remove(set)} aria-label={`Delete ${set.name}`}><Trash2 size={14} /></button></div></article>)}</div> : <div className="consistency"><div className="eyebrow">Empty library</div><p className="muted">Create a set, then add generated icons from the workspace.</p><button className="btn btn-primary" onClick={() => setDialogOpen(true)}><Plus size={15} /> Create set</button></div>}</div>{dialogOpen && <div className="modal-backdrop" role="presentation"><form className="modal" onSubmit={createSet}><div className="eyebrow">New collection</div><h2 className="property-title">Create icon set</h2><label className="field-label" htmlFor="set-name">Set name</label><input id="set-name" className="textarea" value={name} onChange={(event) => setName(event.target.value)} autoFocus required /><label className="field-label" htmlFor="set-description">Description <span className="muted">(optional)</span></label><textarea id="set-description" className="textarea" value={description} onChange={(event) => setDescription(event.target.value)} /><div className="modal-actions"><button type="button" className="btn" onClick={() => setDialogOpen(false)}>Cancel</button><button className="btn btn-primary" type="submit">Create set</button></div></form></div>}</main>;
}
